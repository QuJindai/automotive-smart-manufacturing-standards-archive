name: OpenSTD Verdict - SECOND STANDARD BLOCKED
on:
  push:
    branches: [test/openstd-session-reuse-clean-20260902]
    paths: [state/openstd-session-reuse/verdicts/SECOND_STANDARD_BLOCKED_AFTER_FIRST.trigger]
jobs:
  signal:
    runs-on: ubuntu-latest
    steps:
      - run: echo SECOND_STANDARD_BLOCKED_AFTER_FIRST
